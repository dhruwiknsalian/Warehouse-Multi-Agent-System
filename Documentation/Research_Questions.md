# Research Questions

These guide the experiments and the eventual report/paper. Answer with data, not intuition.

1. How many robots (anchor points) give the best trade-off between payload stability and system cost/complexity?
2. How does cable length affect payload stability (sway, settling time)?
3. How much time is saved using the multi-robot CDPR vs. a single robot carrying the payload directly?
4. What happens to payload control if one robot/anchor fails mid-task? Can the system degrade gracefully with 3 anchors instead of 4?
5. How does obstacle density in the warehouse floor plan affect travel time and path replanning?
6. How does payload mass/shape affect achievable cable tension distribution?

## Reading list (fill in as you go)
- Why ROS 2 over ROS 1 —
- Why Gazebo Harmonic —
- Why Nav2 —
- Why ZigBee for robot-to-robot comms —
- Why cable-driven transport over AGV/conveyor —
- Why multiple robots instead of one —

(Answer each in 2-4 sentences once researched — these become part of your viva prep.)
